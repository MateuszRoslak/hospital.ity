<div class="card">
  <h3 class="card-header">New ward / <%= @floor.name %> floor / <%= @building.name %> building</h3>
  <div class="card-body">
    <%= render 'form', ward: @ward %>
    <br/>
    <%= link_to 'Back', building_floor_wards_path, class: "btn btn-outline-secondary text-dark" %>
  </div>
</div>